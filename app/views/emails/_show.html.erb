<h1 class="text-center"><%= email.object %></h1>
<hr>
<p> <%= email.body %> </p>
<hr>

<%= link_to "Supprimer", email_path(email.id), method: "delete", class:"btn btn-danger ", remote: true%>
<%= link_to "Marquer comme non lu", edit_email_path(email.id), class:"btn btn-info ", id: "read_button", remote: true%>
